# 落地实现 · 详细版 —— 从想法到能跑的系统

> **这份文档的定位**：照着它，一个没参与过讨论的人应该能把整套系统搭起来。
>
> **和其他三份的关系**：
>
> | 文档 | 回答什么 | 读者 |
> |---|---|---|
> | `memory压缩.md` | **为什么这么设计**（研究方案、公式、实验设计） | 自己 / 导师 |
> | `方案讲解_通俗版.md` | **这是个什么东西**（零公式，讲给外行听） | 组会 / 非同行 |
> | **本文** | **怎么把它变成能跑的代码**（模块、接口、接入点、验收） | 实现者（包括三个月后的自己） |
> | `落地实现方案.md` | 本文的旧版（更简略，保留作对照） | — |
>
> **每一个步骤都按同一个格式写**：
>
> **① 这一步要干什么**（目的）→ **② 具体怎么做**（过程，含代码/命令）→ **③ 做完得到什么、怎么确认它是对的**（结果与验收）

---

## 进度地图 —— 先看这个

在读细节之前，先知道现在站在哪里。

```
╔═══ 已完成（代码 + 结论都有）═══════════════════════════════════╗
  [核心压缩模块]       ✅ vtree_compressor.py, 自测通过
  [CA 模块]            ✅ credit.py, 7 个自测场景全过
  [实验① 压缩不丢信息] ✅ 已跑完, 结论确立
  [实验③ 认得出处境]   ✅ 已跑完, 结论确立
  [实验④ 信用集中度]   ✅ 已跑完, 部分结论
  [成本实测]           ✅ bench_probe / bench_summarize
╚════════════════════════════════════════════════════════════════╝

╔═══ 脚本就绪, 有 GPU 就能跑（本机已 dry-run 跑通全流程）═══════╗
  [实验② 信号可信度]   🔧 signal_check_test.py    ← 【地基, 优先级最高】
  [实验⑤ CA 消融]      🔧 ca_ablation_test.py
  [★ 多线索 T-maze]    🔧 tmaze_multiclue_test.py
  [§7.4 判据预检]      🔧 token_criteria_check.py  （非主线, 排最后）
╚════════════════════════════════════════════════════════════════╝

╔═══ 还没做 ═════════════════════════════════════════════════════╗
  [M1 真实 memory]     ⬜ ← 【进论文的证据必须从这来】
  [接进 verl-agent]    ⬜
  [跑训练]             ⬜
╚════════════════════════════════════════════════════════════════╝
```

**一句话概括现状**：**压缩这一半已经立住了；信用分配那一半代码写好了，但一个数都还没跑出来。**

> **关于第二块的脚本**：四个脚本都在本机用假模型做过 dry-run，**全流程跑通**——这只能保证没有低级错误（维度、变量名、空子集），**不保证结论**。真实数字要上 GPU 才有。

⚠️ 这一点在读下文时请始终记着：2.5 / 2.6 两个模块（软分组、信用分配）**代码已经写好了（`credit.py`），但还没在真实数据上跑过** —— 只过了构造场景的自测。所有关于它们效果的说法仍是设计，不是结论。

---

# 第一部分 · 系统长什么样

## 1.1 一句话

**边走边把记忆压缩掉，跑完再用压缩后的记忆去算每一步的功劳。**

两件事，一前一后，靠一个叫「保真度」的东西连起来。

## 1.2 全图

```
╔═══════════ 阶段 A：rollout 内部，每走一步都跑一遍 ═══════════╗
║                                                              ║
║   环境                                                       ║
║    │ obs_t（这一步看到什么）                                 ║
║    ▼                                                         ║
║  ┌────────────────┐                                          ║
║  │ ① ValueProbe   │  问 actor："这任务能成吗？"              ║
║  │   （不生成）    │  → 把握值 V_t ∈ (0,1)                    ║
║  └───────┬────────┘  成本：决策的 13.9%（复用 KV cache 后）   ║
║          │ V_t                                               ║
║          ▼                                                   ║
║  ┌────────────────┐                                          ║
║  │ ② 算跳幅        │  Δ_t = |V_t − V_{t−1}|                  ║
║  └───────┬────────┘  成本：一次减法                          ║
║          │ Δ_t                                               ║
║          ▼                                                   ║
║  ┌────────────────┐                                          ║
║  │ ③ 分配分辨率    │  固定 token 上限内，按性价比贪心升档     ║
║  │  （纯 CPU）     │  每步都【重排】，不是增量追加            ║
║  └───────┬────────┘  成本：一次排序，可忽略                  ║
║          │                                                   ║
║          ├──────────► memory_t（压缩后的文本）               ║
║          │                    │                              ║
║          │                    ▼                              ║
║          │             ┌─────────────┐                       ║
║          │             │ actor LLM   │ ──► a_t ──► 环境      ║
║          │             └─────────────┘                       ║
║          │                                                   ║
║          └──────────► 保真度 c_t ∈ (0,1]  ★这个存下来        ║
║                            （这一步的记忆被压得多狠）        ║
╚══════════════════════════════════════════════════════════════╝
                              │
                     轨迹跑完，拿到总回报 R
                              │
╔═══════════ 阶段 B：rollout 之后，整批一起算 ═══════════════╗
║                                                            ║
║  所有步的 (memory_t, a_t, R, c_t)                          ║
║          │                                                 ║
║          ▼                                                 ║
║  ┌────────────────┐                                        ║
║  │ ⑤ 编码 φ       │  memory 文本 → 向量                    ║
║  │                │  ⚠️ 必须用 last-token 池化（实测）      ║
║  └───────┬────────┘                                        ║
║          ▼                                                 ║
║  ┌────────────────┐                                        ║
║  │ ⑥ SoftGrouper  │  按向量相似度给权重（软分组）           ║
║  │                │  w_ij = exp((S_ij − 1) / τ)             ║
║  └───────┬────────┘                                        ║
║          ▼                                                 ║
║  ┌────────────────┐                                        ║
║  │ ⑦ 保真度加权    │  w' = w × c  ← ★阶段 A 存的东西在这用   ║
║  └───────┬────────┘  （⚠️ 这一步可能没用，见 2.6）          ║
║          ▼                                                 ║
║  ┌────────────────┐                                        ║
║  │ ⑧ 算优势 A      │  A = Q̂(s,a) − V̂(s)                    ║
║  └───────┬────────┘                                        ║
║          ▼                                                 ║
║  ┌────────────────┐                                        ║
║  │ ⑨ GRPO 更新     │  拿 A 去更新 actor                     ║
║  └────────────────┘                                        ║
╚════════════════════════════════════════════════════════════╝
```

**要记住的两条**：

1. **①–④ 在 rollout 里，每步都跑**；**⑤–⑨ 在 rollout 后，整批跑一次**。
2. **保真度 c_t 是阶段 A 顺手产出的**——不用额外算，它就是「这一步最后落在哪一档」的直接结果。这是两个阶段之间唯一的额外接口。

## 1.3 走一遍具体数字

光看图不够，走一条真实的短轨迹。任务：**把番茄加热后放到桌上**。token 上限设 40（很紧，为了看出效果）。

### 第 1 步

```
obs_1 = "你在厨房里四处找番茄"
```

- **①** 探针输入 = `"你在厨房里四处找番茄"`，问「能成功吗」→ V_1 = 0.35
- **②** Δ_1 = |0.35 − 0.5| = **0.15**（0.5 是先验起点）
- **③** 只有一步，且是当前步 → 必然 FULL
- **④** memory_1 = `"你在厨房里四处找番茄"`，保真度 = 1.0

### 第 2 步

```
obs_2 = "你环顾四周，没看到别的东西"
```

- **①** 探针输入 = **memory_1 + obs_2**（注意：是上一步的**压缩结果**，不是全量原文）→ V_2 = 0.34
- **②** Δ_2 = |0.34 − 0.35| = **0.01**（几乎没跳 = 废话）
- **③** 当前步（第 2 步）必然 FULL。第 1 步要不要升档？

  性价比 = 跳幅 ÷ 原文长度 = 0.15 ÷ 10 字 = 0.015。预算够，升成 FULL。
- **④** memory_2 = 两句原文，保真度 = 1.0

### 第 5 步（预算开始不够了）

假设前四步累积下来：

| 步 | 原文 | 长度 | Δ | 性价比 |
|---|---|---|---|---|
| 1 | 你在厨房里四处找番茄 | 10 | 0.15 | 0.015 |
| 2 | 你环顾四周，没看到别的东西 | 13 | 0.01 | 0.0008 |
| 3 | 在料理台找到并拿起了生的番茄 | 14 | 0.20 | **0.014** |
| 4 | 你听到冰箱压缩机启动的声音 | 13 | 0.00 | 0 |
| **5** | 手拿生的番茄，走到微波炉前 | 13 | 0.05 | — |

**③ 的贪心过程**（预算 40，当前步第 5 步的 13 字**不占**这个预算）：

```
初始：全部 MERGE，只有第 5 步是 FULL
      成本 = "（此处 4 步例行操作已折叠）" ≈ 14 字   剩余 40 − 14 = 26

按性价比排序：步1(0.015) > 步3(0.014) > 步2(0.0008) > 步4(0)

试 步1 → FULL：
      "你在厨房里四处找番茄" + "（此处 3 步...）" = 10 + 14 = 24 ≤ 40  ✅ 接受

试 步3 → FULL：
      10 + 14(折叠步2) + 14(步3原文) + ... 超了 ❌
试 步3 → SUMM（摘要，目标 14×0.45 ≈ 6 字 → "找到并拿起番茄"）：
      10 + 14 + 7 = 31 ≤ 40  ✅ 接受

试 步2：Δ=0.01 ≥ min_jump(0.01)，勉强试；预算只剩 9，塞不下 ❌
试 步4：Δ=0.00 < min_jump → 【break，直接停】
        ← 剩下的都是零信息步，预算宁可剩着也不浪费在它们身上
```

**④ 最终输出**：

```
你在厨房里四处找番茄            ← FULL   (Δ=0.15)
(此处 1 步例行操作已折叠)        ← MERGE  (步2)
找到并拿起番茄                   ← SUMM   (Δ=0.20, 但原文长, 只够摘要)
(此处 1 步例行操作已折叠)        ← MERGE  (步4)
手拿生的番茄，走到微波炉前       ← FULL   (当前步, 恒保留)
```

**保真度**（按原文长度加权平均档位分）：

```
c = (10×1.0 + 13×0.3 + 14×0.6 + 13×0.3 + 13×1.0) ÷ (10+13+14+13+13)
  = (10 + 3.9 + 8.4 + 3.9 + 13) ÷ 63
  = 39.2 ÷ 63
  = 0.62
```

**这个 0.62 就是要存下来喂给阶段 B 的东西。** 它说：这条记忆压得挺狠的，评价它时打个六折。

### 从这个例子能看出三件事

1. **不是"重要的就留原文"**——步 3 跳幅最大（0.20），却只拿到摘要档，因为它原文长（14 字），性价比反而不如步 1。这是**性价比排序**而非纯跳幅排序的直接后果。
2. **零信息步会触发 break**，预算剩着也不给它们。早期版本没这个阈值，贪心会把预算浪费在 Δ=0 的步上。
3. **当前步永远 FULL 且不占预算**——否则当前观测一长（比如网页全文），就会把历史的关键步全挤掉，出现「预算越紧越丢关键信息」的反向行为。这是自测揪出来的 bug。

## 1.4 为什么必须【在线】压

### ① 目的

这是**唯一一条决定整体架构的约束**，先定下来，后面所有设计都被它锁死。

问题是：压缩到底放在哪？两个选项——

| 选项 | 做法 |
|---|---|
| **事后压** | agent 决策时看完整历史；轨迹跑完再压缩，压缩结果只用来算信用 |
| **在线压** | agent 决策时看到的就是压缩后的 memory；边走边压 |

### ② 推理过程

回到两条动机：

- **动机一（省 token）**：context 有硬上限，长轨迹必然超限；
- **动机二（提纯信号）**：冗余会主动干扰判断。

**如果选「事后压」**：

- agent 决策时仍然读全量历史 → **照样超 context** → **动机一完全失效**；
- agent 决策时仍然被冗余干扰 → **动机二只兑现一半**（只有算信用时干净了，做决策时还是脏的）。

### ③ 结论

**压缩必须作用在 agent 决策所看的 memory 上 ⇒ 必须在线压。**

这条把下面三件事全定死了：

| 被锁死的 | 因为 |
|---|---|
| 探针必须能**增量**算 | 每步都要出一个 V_t，不能等轨迹跑完 |
| token 重分配必须**便宜** | 每步都要重排一次 |
| 摘要**不能同步阻塞决策** | 决策在等它 |

**实测把这三条的分量测出来了**（详见第三部分）：探针**必须复用 KV cache**（48% → 13.9%）；摘要走**零成本截断或抽取式**（4.3%），生成式（70%）太贵。

---

# 第二部分 · 逐个模块

每个模块按同一格式：**目的 → 输入输出 → 实现 → 现状 → 怎么验 → 坑**。

---

## 2.1 `ValueProbe` —— 把握值从哪来

### ① 目的

给出一个数：**在当前记忆下，这个任务最终能成功的把握有多大**。整套方法都建立在它上面——如果这个数是噪声，后面全废。

### ② 输入输出

```
输入：memory 文本（一条或一批）、任务描述
输出：一个 0~1 的小数（或一批）
```

### ③ 怎么实现

**核心技巧：不让模型生成，只读它的倾向。**

拼一个填空题：

```
任务：{task}
当前记忆：
{memory}
问：这个任务最终能成功吗？只回答一个字：是 或 否。
答：
```

送进模型做**一次 forward**，取最后一个位置的 logits，只看「是」和「否」两个 token：

```python
lg = model(**enc).logits[:, -1, :].float()          # 最后一位的全词表分数
p  = torch.softmax(lg[:, [YES_ID, NO_ID]], -1)[:, 0]  # 只在"是/否"之间归一化
```

**为什么这样比让它生成好**：

| | 原因 |
|---|---|
| **快** | 不 decode。实测：写 1 个字的工夫够读 30+ 个字 |
| **准** | 只在「是/否」之间比，排除了模型想说「这个」「应该」的干扰 |
| **连续** | 得到 0.70 而非硬邦邦的「是」——**跳幅全靠这个连续性才算得出来** |

**实际代码**：`vtree_compressor.py` 的 `ValueProbe` 类（第 35 行起）。

### ④ 现状

✅ **已实现且跑通**。另外提供两个替身用于无 GPU 测试：

| 类 | 用途 |
|---|---|
| `ValueProbe` | 真的问模型（要 GPU） |
| `ConstProbe` | 命中关键词就给高分——**做单元测试用**，不用加载模型 |
| `ScriptedProbe` | 直接喂一串预设值——**做确定性测试用**，结果可复现 |

### ⑤ 怎么验证它是对的

⬜ **实验②「信号可信度检验」——还没做**。要验的是：

> 跳幅大的步，是不是真的重要？

做法：在**真实环境**里，把跳幅最大的那一步的动作**换成随机动作**，看成功率掉多少；再随机挑一步做同样的事作对照。**跳幅步掉得更多 ⇒ 信号站得住**。

**出口条件**：AUC > 0.8。

### ⑥ 已知的坑

**坑 1：训练中途探针会漂。**

探针用的是 actor 自己。actor 一更新，它对「能不能成」的判断标准就变了，**V_t 的尺度会漂移，跳幅失去可比性**。

**必须做的**：训练开始时 `deepcopy` 一份 actor 权重冻结当探针，或者用独立的小模型。

> ⚠️ **这一条不做，跳幅信号会在训练中途悄悄失效**，而且不会报错——你只会看到效果莫名其妙变差。

**坑 2：这个值是主观的，不是真实概率。**

模型倾向于说「能行」，绝对值大概率偏乐观。**但不影响方法**——我们只用**相对变化**（跳了多少），不用绝对值。只要它能分出「这步比那步跳得多」就够了。

**坑 3：prompt 措辞敏感。**

「这个任务最终能成功吗」在不同环境下的敏感度不同。**建议**：实验②里顺便扫 2~3 种措辞，选 AUC 最高的固定下来，之后不再改。

---

## 2.2 `VTreeCompressor` —— 核心压缩模块

### ① 目的

在固定的 token 上限内，决定**每一步展开到多细**，输出压缩后的 memory 和保真度。

### ② 输入输出

```
输入（每步）：这一步的观测文本
输出（随时可取）：(压缩后的 memory 文本, 每步的档位分列表)
                  另外可取：整段保真度（一个数）
```

### ③ 怎么实现

**三档定义**：

```python
RES_FULL, RES_SUMM, RES_MERGE = 0, 1, 2
CONF = {RES_FULL: 1.0, RES_SUMM: 0.6, RES_MERGE: 0.3}
```

> ⚠️ **0.6 / 0.3 是拍脑袋的值，没有标定过。** 这是一个已知的开放问题（见第八部分）。

**两个方法**：

```python
c = VTreeCompressor(probe, tok, budget=512, task="把番茄加热后放到桌上")
c.push(obs)          # 每走一步调一次 —— 1 次探针调用
mem, conf = c.render()   # 随时可调 —— 纯 CPU，无模型调用
```

#### `push` 干了什么

```python
def push(self, obs_text):
    probe_in = (self._last_mem + "\n" + obs_text).strip() if self._last_mem else obs_text
    v = float(self.probe([probe_in], self.task)[0])
    self.steps.append(obs_text)
    self.values.append(v)
    self.jumps.append(abs(v - self.v_prev))
    self.v_prev = v
    return self.jumps[-1]
```

**注意 `probe_in` 这一行**——探针的输入是**上一步的压缩 memory + 新观测原文**，**不是全量原文**。三个理由：

1. **自洽**：全量原文本身会超 context（压缩就是为了解决这个），用它算价值自相矛盾；
2. **与部署一致**：agent 看到的就是压缩后的 memory，价值判断也该基于同一份输入；
3. **副作用是自纠错**：如果压缩把关键信息丢了，价值判断会随之改变，跳幅也会变——**系统能感知到自己压坏了**。

#### `render` 干了什么（核心算法）

```python
n = len(self.steps)
res = [RES_MERGE] * n          # 起点：全部折叠
res[n-1] = RES_FULL            # 当前步恒为原文
cur_cost = self._n_full[n-1]   # ★当前步的开销单独记，不占历史预算

for i in sorted(range(n-1),
                key=lambda k: (-self.jumps[k] / max(self._n_full[k], 1), -k)):
    if self.jumps[i] < self.min_jump:
        break                              # ★零信息步：直接停，不浪费预算
    for target in ((RES_FULL, RES_SUMM) if self.tiers == 3 else (RES_FULL,)):
        if target >= res[i]:
            continue                       # 只升不降
        trial = res.copy(); trial[i] = target
        if self._cost(trial) - cur_cost <= self.budget:
            res = trial
            break
```

**四个设计点，每一个都是自测踩出来的**：

**（a）排序键是「性价比」，不是纯跳幅**

```python
key = -self.jumps[k] / max(self._n_full[k], 1)
       ↑跳幅            ↑原文 token 数
```

**为什么**：一步跳幅再大，如果要吃掉半个预算，可能不如换两个次要但便宜的步。走查里见过实例：Δ=0.07（19 字）被压掉，Δ=0.05（13 字）反而保住了原文——**这是对的**，因为总信息量更大。

**（b）tie-break 靠后优先**（`-k`）

**为什么**：平局时如果不指定，Python 的稳定排序会让**最早的步永远胜出**。近的步通常更相关，所以平局时选靠后的。

**（c）`min_jump` 阈值 + `break`**

**为什么**：没有它，贪心会把预算花在 Δ=0 的流水账上（因为它们也在排序列表里，只是排在后面）。加了阈值后，**预算宁可剩着**也不浪费。

注意是 `break` 不是 `continue`——列表已按跳幅降序，遇到第一个低于阈值的，后面全都是。

**（d）当前步的开销不占历史预算**

```python
if self._cost(trial) - cur_cost <= self.budget
                      ↑减掉当前步
```

**为什么**：不这么做的话，当前观测一长（网页全文、长指令），就会把历史的关键步全挤掉，产生**「预算越紧越丢关键信息」的反向行为**——这是最严重的一个 bug，自测才发现。

#### 保真度怎么算

```python
def fidelity(self):
    _, conf = self.render()
    w = np.asarray(self._n_full, dtype=float)   # 按原文长度加权
    c = np.asarray(conf, dtype=float)
    return float((w * c).sum() / max(w.sum(), 1e-9))
```

**为什么是「整段」而不是「某一步的档位」**：

1. 分组用的表示是**整段 memory**（该步当时的历史前缀），所以它的可靠性取决于**整段被压得多狠**；
2. 当前步恒为 FULL，若取单步档位，**保真度恒等于 1.0，完全没有区分度**。

**为什么按原文长度加权**：长的步被压掉，丢的信息更多。

### ④ 现状

✅ **已实现、自测通过、三个实验都在用**。文件：`vtree_compressor.py`（约 380 行）。

还提供一个离线批量入口：

```python
mem, conf = VTreeCompressor.compress(steps, probe, tok, budget=80, task=task)
# batch_probe=True 时把所有前缀打包问，比逐步 push 快很多
```

### ⑤ 怎么验证它是对的

✅ **实验① 已跑完**，结论确立：

| 走廊长 | 什么都不记 | 全部记住 | **本方法** | 只留最近2步 | 只留最近8步 |
|---|---|---|---|---|---|
| 1 | 51% | 100% | **100%** | 100% | 100% |
| 2 | 45% | 100% | **100%** | **45%** ⬇ | 100% |
| 8 | 51% | 100% | **100%** | 51% | **51%** ⬇ |
| 16 | 52% | 100% | **100%** | 52% | 52% |

**token 占用**：全部记住随任务变长暴涨（40 → 301），**本方法基本不涨（恒 40）**，省 7.5×。

### ⑥ 已知的坑

**坑 1：预算过紧时会静默丢关键信息。**

已加告警：如果跳幅最大的历史步仍被折叠，`self.budget_warning` 会被设上。**接进训练时要把这个告警接到日志上**，否则丢了都不知道。

**坑 2：三档在预算极紧或极松时退化成两档。**

实测：预算 200/140（充足）和 50（极紧）时，三档和两档表现完全一样。**中间档只在「差一点就装得下」时才有价值**（实测在预算 110 和 70 时各多保一条线索）。

**这不是 bug，是这个设计的固有边界，写论文时要诚实说明。**

---

## 2.3 `Summarizer` —— 中间档怎么生成

### ① 目的

把一段观测压成更短的一句，供 SUMM 档使用。

### ② 这里踩过一个大坑，值得单独讲

**最初的做法**：手写正则模板，理由是「在线不能为了摘要去调 LLM」。

**问题**：那意味着**换一个环境就要重写一整档**。三档之一要人工适配，方法就**没有通用性可言**——这是审稿人一眼就会挑出来的死穴。

**问题的根源是把两件事混成了一件**：

| | 是什么 | 谁的活 |
|---|---|---|
| **「这一段该压多短」** | 按价值跳幅分配 token 上限 | ★ **本方法的创新** |
| **「怎么把一段话压短」** | 文本压缩 | **已有成熟通用方案**（LLMLingua、Selective-Context，或直接让 LLM 摘要） |

**正确做法：第二件做成可换的组件。**

### ③ 怎么实现

```python
class Summarizer:                    # 接口：把 text 压到 <= budget_chars，自带缓存
    def __call__(self, text, budget_chars) -> str: ...

class TruncSummarizer(Summarizer):   # 【默认】保首句主干 + 截断。零成本、零依赖
class LLMSummarizer(Summarizer):     # 通用：让 LLM 摘要。质量好但贵（70% 决策耗时）
class RuleSummarizer(Summarizer):    # 手写正则：【不通用】，只作消融基线
```

**目标长度 = `ratio × 原文长度`**（默认 ratio=0.45），**必须保证压缩比**：

```python
def _summ(self, i):
    src = self.steps[i]
    return self.summarizer(src, max(8, int(len(src) * self.summ_ratio)))
```

> ⚠️ **不这么做的后果**（自测踩到过）：`RuleSummarizer` 匹配不上时会退化成原样返回，**摘要 ≈ 原文，S 档形同虚设，三档塌成两档**。

### ④ 现状

✅ **接口和三个实现都已写好**。默认用 `TruncSummarizer`。

### ⑤ 怎么验证正交性成立

✅ **已验证**（`vtree_compressor.py` 自测场景 3）：

`TruncSummarizer` 和 `RuleSummarizer` 跑同一条轨迹，**档位序列完全一致**（`FMMFMMSMMF`）。

**这说明：换摘要器不影响分辨率分配。** 论文里可以说——*摘要器与本方法正交，我们贡献的是分辨率分配策略*。**通用性问题消失，创新点反而更聚焦。**

### ⑥ 已知的坑

**坑：抽取式的质量没验过。**

抽取式（只读不写，从原文挑词）速度赢麻了（比生成式快 8.6×），但它产出的是**碎片文本**（比如「台面上…番茄」），**agent 读不读得懂是另一回事**。

**速度问题解决了，质量问题原封不动。**

**推荐顺序**：先用零成本截断跑通 → 若质量不够，上抽取式 → 生成式只在离线场景考虑。

---

## 2.4 保真度 —— 两个阶段之间的接口

### ① 目的

告诉阶段 B：**这条记忆有多可信**。压得狠的可能已经丢了要紧的东西，算功劳时该打折。

### ② 怎么来的

**零额外成本**——它就是压缩过程的副产品：

```
每步落在哪一档  →  查 CONF 表（1.0 / 0.6 / 0.3）
                →  按原文长度加权平均
                →  一个 0~1 的数
```

### ③ 现状

✅ 已实现（`fidelity()`）。

### ④ ⚠️ 但它可能没用

在 `walkthrough.py` 的端到端演示里，开不开保真度加权的差别是：

```
1.438  →  1.439      （差 0.001，约等于没有）
```

**原因分析**：软分组（2.5）已经把压得狠的记忆排除得差不多了——它们的表示本来就不像，权重自然低。保真度加权**无事可做**。

**结论**：这一条要在实验⑤里专门验。**如果确认没用，就该删掉**——方案少一个部件反而更干净、更好讲。

### ⑤ 两个还没定的问题

| 问题 | 现在的做法 | 疑问 |
|---|---|---|
| **档位分怎么定** | 常数 1.0 / 0.6 / 0.3 | 拍脑袋的。是不是该按实例算（比如实际压缩比）？ |
| **「压得多狠」该怎么量** | 档位 + 长度加权 | 用**语义相似度**衡量压缩前后的差距行不行？ |

**第二个问题已经有明确答案：不能用语义相似度。**

反例：原文「打开微波炉，把生的**番茄**放了进去」，压成「打开微波炉，把生的**茄子**放了进去」——**语义相似度极高**（只差一个字），但对任务成败的影响**完全不同**。

借 bisimulation 的精神：该看的是「**压缩前后，对未来的影响是否一样**」，而不是「**看起来像不像**」。具体怎么算，没定。

---

## 2.5 `SoftGrouper` —— 软分组

> ✅ **代码已写**：`credit.py` 的 `SoftGrouper`。⚠️ 但只过了构造场景的自测，**没在真实数据上跑过**。

### ① 目的

**把「处境相似」的步凑成一堆**，好在堆内做对比。

**为什么需要**：算功劳靠对比——同一个处境下，做 A 和做 B 谁的结果好。**前提是先能认出「这几次是同一个处境」**。

### ② 为什么不能沿用现成做法

GiGPO 用的是**精确哈希分组**：状态字符串完全一致才算同一组。

**压缩之后这条直接失效**——压缩是有损的，同一个处境在两条轨迹里可能被压成不同的文本，哈希对不上。

### ③ 怎么做

**改成按「像的程度」给权重**，不做一刀切：

```python
class SoftGrouper:
    def weights(self, phi, tau=0.1):
        S = phi @ phi.T                          # phi 已 L2 归一化 ⇒ S 就是余弦相似度
        W = np.exp((S - 1.0) / tau)              # 越像权重越大，指数衰减
        np.fill_diagonal(W, 0.0)                 # 不把自己算进去
        return W / W.sum(1, keepdims=True).clip(1e-8)
```

**τ 控制「多像才算像」**：

- τ 小 → 只有非常像的才有权重（组很紧，但样本少）
- τ 大 → 不太像的也算进来（样本多，但可能混入无关处境）

### ④ ⚠️ 一个关键的实现细节：池化方式

**编码 memory 时必须用 last-token 池化，不能用 mean 池化。** 这是实测出来的，差别是决定性的：

| 池化方式 | 同处境相似度 | 不同处境相似度 | **差距** |
|---|---|---|---|
| mean | 0.997 | 0.984 | **+0.013** ❌ |
| **last-token** | — | — | **+0.401** ✅ |

**为什么这个差距要命**：核加权 `exp((S−1)/τ)` 吃的是**绝对差距**。差距只有 0.013 时，同处境和不同处境的权重几乎一样——**软分组等于没做**。

> **这里踩过一个坑**：一开始只看 sep-AUC（判别能力），mean 池化的 AUC 是 0.863，**看起来完全合格**。但 AUC 只看**排序**，排序对**不等于拉得开**。
>
> 打个比方：三个人身高 180.0 / 179.9 / 179.8 cm，**排名完全正确**（AUC = 1.0），但你说得出谁高谁矮吗？
>
> **真正的判据是「差距」那一列，不是 AUC。**

判读标准：

| 差距 | 说明 |
|---|---|
| < 0.05 | 向量全挤在窄锥里（各向异性），**软分组必然退化，先修表示** |
| > 0.3 | 拉得开，核加权能正确集中 ✅ |

### ⑤ 现状与验证

✅ **表示层已验证**（实验③ `sep_auc_test.py`）：压缩后的 sep-AUC 是 **0.997**，未压缩是 0.998——**几乎无损**。

> **这条结论很重要**：它证明「压缩不损害处境识别能力」，是软分组能工作的前提。

✅ **`SoftGrouper` 已写**（`credit.py`），自测覆盖：同处境权重占比 → 1.000、相似度差距 +1.120、有效邻居数随 τ 单调上升。

**多了一个原设计没有的参数 `block`**：给了轨迹 id 就屏蔽同轨迹内的邻居。

> **为什么必须有**：同一条轨迹里的所有步共享同一个回报 R，拿它们互相当对照等于自己跟自己比，V̂ 会被自身回报污染。**跨轨迹对比才是信用信号的来源。**

⬜ **还没在真实数据上跑过。**

### ⑥ 还没定的：τ 该往哪调

**两个方向都说得通，所以两个都要进消融**：

| 方向 | 理由 |
|---|---|
| **τ 放大**（放宽） | 压缩后表示有噪声，标准太严会导致组内样本太少 |
| **τ 缩小**（收紧） | 压缩后向量更容易挤在一起，标准不收紧会把无关处境混进来 |

**不预设答案，扫一遍 τ 看有效邻居数**（`sep_auc_test.py` 里已有这个诊断）。

---

## 2.6 `CreditAssigner` —— 算功劳

> ✅ **代码已写**：`credit.py` 的 `CreditAssigner`。⚠️ 但只过了构造场景的自测，**没在真实数据上跑过**。

### ① 目的

把一条轨迹的**一个总分**，摊到**每一步**头上。

### ② 怎么做

```python
class CreditAssigner:
    def advantages(self, W, conf, returns, actions, use_conf=True, use_pace=True):
        if use_conf:                                   # ② 压得粗的邻居降权
            W = W * conf[None, :]
            W = W / W.sum(1, keepdims=True).clip(1e-8)

        V = W @ returns                                # V̂(s) = 处境的平均水平

        if not use_pace:
            return returns - V

        Q = np.zeros_like(V)                           # ③ 同动作邻居 → Q̂(s,a)
        for i in range(len(V)):
            m = (actions == actions[i]); m[i] = False
            Q[i] = (W[i] * m) @ returns / max((W[i] * m).sum(), 1e-8) if m.any() else returns[i]
        return Q - V
```

**逐行解释**：

| 行 | 在算什么 | 大白话 |
|---|---|---|
| `W = W * conf` | 保真度加权 | 压糊了的记忆，参考价值打折 |
| `V = W @ returns` | 处境的**平均**结果 | 「在这种处境下，一般能拿多少分」 |
| `Q[i] = ...` | 处境下**做了这个动作**的平均结果 | 「在这种处境下，做了这件事能拿多少分」 |
| `return Q - V` | **优势** | 「做这件事，比平均水平好多少」 |

**举例**：处境是「手拿生番茄站在微波炉前」。

| 这一遍做了什么 | 结果 |
|---|---|
| 放进微波炉 | 成功 |
| 放进微波炉 | 成功 |
| 走开去拿盘子 | 失败 |
| 放进微波炉 | 失败（后面把番茄摔了） |

```
V̂（这个处境的平均）    = (1 + 1 + 0 + 0) / 4 = 0.50
Q̂（放进微波炉的平均）  = (1 + 1 + 0)     / 3 = 0.67
优势 = 0.67 − 0.50 = +0.17   →  「放进微波炉」这一步该记功
```

### ③ 两个开关就是消融的四条臂

```
use_conf=F, use_pace=F   →  a 臂：纯软分组
use_conf=F, use_pace=T   →  b 臂：+ 动作反事实
use_conf=T, use_pace=T   →  c 臂：+ 保真度加权（完整版）
use_conf=T, use_pace=F   →  d 臂：只加保真度
```

**消融不用另写代码**，改两个 bool 就行。

### ④ 现状

✅ **代码已写**：`credit.py`。与 `credit_rank_test.py` 里的内联实现**数值完全等价**（自测场景 5，最大逐元素差 0.00e+00）——换用这个模块不会改变已跑出的数字。

🔧 **实验⑤脚本已写待跑**：`ca_ablation_test.py`。

**跑之前它会先做两个前置诊断**，任一不过就别解读主结果：

| 诊断 | 不过意味着 |
|---|---|
| **conf 的方差** | 方差≈0 时 `use_conf` 退化成「乘常数再归一化」= **恒等变换**，c≈b 是**实验设计不成立**，不是「保真度加权没用」 |
| **分组质量的差距** | 差距 < 0.05 时软分组必然退化，测什么都没意义 |

> **第一条是写脚本时才意识到的**：如果所有轨迹的压缩程度差不多，这个消融**数学上必然测不出差异**。所以脚本里 `BUDGETS` 是**轮换**的（40/60/90/140），故意让 conf 有方差。dry-run 实测 conf 标准差 0.208，有区分度。

### ⑤ 已知的隐忧

**保真度加权（`use_conf`）大概率没用**——见 2.4。实验⑤的第一要务就是确认这一点，**该砍就砍**。

---

# 第三部分 · 成本：实测数字

## 3.1 为什么要专门测

### ⚠️ 一次估错一个数量级的教训

我原先的推理是：

> decode 是瓶颈 → 探针不 decode → 所以探针只占 1~3%

**实测：48%。**

**错在哪**：在**长记忆**场景下，**prefill（读）才是主成本**，不是 decode（写）。记忆越长，读的开销越大，而探针要把整段记忆重读一遍。

**这条推理链读起来很顺——越顺的推理越容易蒙混过关，因为它没有卡点。**

## 3.2 怎么测的

**脚本**：`bench_probe.py`、`bench_summarize.py`
**环境**：Qwen2.5-7B-Instruct（4bit 量化），单条推理，取 10 次重复的中位数

对比四种操作在不同记忆长度下的耗时。

## 3.3 探针成本：结果

| 记忆长度 | ① 决策<br>(读全部+写30字) | ② 探针·**重新读** | ③ 探针·**接着读** | ④ LLM 摘要<br>(读1条+写1句) |
|---|---|---|---|---|
| 200 | 1341 ms | 420 ms (31.3%) | **251 ms (18.7%)** | 1293 ms (96.4%) |
| 500 | 1707 ms | 746 ms (43.7%) | **263 ms (15.4%)** | 1339 ms (78.4%) |
| 1000 | 2184 ms | 1101 ms (50.4%) | **275 ms (12.6%)** | 1345 ms (61.6%) |
| 2000 | 3046 ms | 2029 ms (66.6%) | **276 ms (9.1%)** | 1414 ms (46.4%) |
| **平均占比** | — | **48.0%** ❌ | **13.9%** ✅ | **70.7%** ❌ |

### 顺便反推出读和写的单价（用 1000 token 那行）

写 30 字的耗时 = 2184 − 1101 ≈ 1083 ms

| | 速度 |
|---|---|
| **读** | 1000 字 ÷ 1101 ms ≈ **900 字/秒** |
| **写** | 30 字 ÷ 1083 ms ≈ **28 字/秒** |

> **写一个字的工夫，够读三十多个字。**
>
> 读可以并行（所有位置同时算），写只能逐字串行（每个新字都要回看前文）。

## 3.4 三条结论

### 结论 1：KV cache 复用从「优化项」升级为【必做项】

| 做法 | 占决策耗时 | 判定 |
|---|---|---|
| **重新读**（探针独立跑一遍） | 48%，且随记忆变长恶化到 66.6% | ❌ 不可接受 |
| **接着读**（复用决策时的 KV cache） | **13.9%**，绝对耗时几乎恒定（251→276 ms） | ✅ 可接受 |

**为什么「接着读」的耗时和记忆长度无关**：记忆部分已经在 cache 里了，探针只需多读那句问话（约 20 token）。

**所以记忆越长，占比反而越低**（2000 token 时仅 9.1%）——**而长程任务正是目标场景**。

> **多花 9%~13% 的时间，换记忆缩小 7.5 倍 ⇒ 划算。**

### 结论 2：中间档——生成式太贵，抽取式救场

第二轮实测（`bench_summarize.py`）推翻了「只能异步」的结论：

| 做法 | bs=1 单条 | **bs=8 单条** | 占决策(≈2000ms) |
|---|---|---|---|
| 生成式 `max_new=48` | 2546 ms | 2269 ms | 113% ❌ |
| 生成式 `max_new=16` | 1043 ms | 729 ms | 36% ❌ |
| **抽取式**（只读不写） | 418 ms | **85 ms** | **4.3%** ✅ |

**抽取式比生成式的批量版快 8.6×** ⇒ **同步做就够，不必异步**。

**为什么批量救不了生成式**：批量能摊薄固定开销（实测 183 ms）和 prefill；但 decode 的 16 步是**串行**的，batch 再大还是 16 步。抽取式没有 decode，所以批量对它效果拔群。

### 结论 3：新增一条风险

⚠️ **KV cache 复用在 verl-agent 里能否实现，还没验证。**

做不到的话有两条退路：

| 退路 | 效果 | 代价 |
|---|---|---|
| **小模型探针**（1.5B 代替 7B） | 成本再降约 4× → 48%/4 ≈ **12%** | 多占一份显存 |
| **让 agent 顺手报**（决策时多输出一行「把握：0.7」） | **零额外调用** | 自评不如独立探针客观 |

---

# 第四部分 · 接进 verl-agent

## 4.1 目的

把上面这些模块塞进现成的训练框架，**不改动训练主循环**。

## 4.2 四个挂载点

| # | 挂载点 | 原来是什么 | 换成什么 |
|---|---|---|---|
| 1 | `agent_system/memory/` | `SimpleMemory`（最近 K 步原样拼接） | **`VTreeMemory`**：包一层 `VTreeCompressor`，`step()` 里 `push` + `render` |
| 2 | `env_manager.build_text_obs` | 拼原始 K-window | 拿 `VTreeMemory.render()` 的结果 |
| 3 | 优势计算（GiGPO step-level） | 精确哈希分组 + 组内归一 | **`SoftGrouper` + `CreditAssigner`** |
| 4 | rollout 数据结构 | `(obs, action, reward)` | 多带两列：**保真度**、**跳幅**（后者便于诊断） |

**改动量估计**：memory 侧一个新类 + 优势计算换一个函数 + rollout 结构加两列。

## 4.3 逐个挂载点怎么改

### 挂载点 1：`VTreeMemory`

**目的**：让 agent 每步拿到的是压缩后的 memory。

**怎么做**：

```python
class VTreeMemory:
    def __init__(self, probe, tok, budget, task):
        self.c = VTreeCompressor(probe, tok, budget=budget, task=task)

    def step(self, obs_text):
        self.c.push(obs_text)                 # 1 次探针调用
        mem, _ = self.c.render()              # 纯 CPU
        return mem

    def fidelity(self):
        return self.c.fidelity()              # 存进 rollout，给阶段 B 用
```

**验收**：随便跑一条轨迹，确认 `len(tokenize(mem))` 不超过 budget + 当前观测长度。

### 挂载点 3：优势计算

**目的**：把哈希分组换成软分组。

**验收**：先跑一个「退化测试」——把 τ 设到极小，软分组应该**退化成近似硬分组**，此时优势值应与原 GiGPO 接近。**对不上说明实现有问题。**

## 4.4 怎么确认没改坏

**三个由浅入深的检查**：

| # | 检查 | 期待 |
|---|---|---|
| 1 | budget 设成很大（比如 100000） | 压缩应该**不生效**，memory ≈ 全量原文，结果应与 baseline 完全一致 |
| 2 | τ 设成极小 | 软分组退化成硬分组，优势值应接近原 GiGPO |
| 3 | 关掉 `use_conf` 和 `use_pace` | 只剩软分组，是最接近 baseline 的一档 |

**这三条都是「退化到已知情况」的检查**，比直接看新指标可靠得多——**新指标好看可能是 bug，退化对不上一定是 bug。**

---

# 第五部分 · 三阶段路线

## 阶段 1 · 离线跑通压缩（不训练）

### ① 目的

**确认压缩本身是对的**——在不涉及训练的前提下，验证压缩没丢关键信息、token 确实省了、价值信号站得住。

### ② 做什么

用已有的合成 / 真实轨迹，把 `VTreeCompressor` 跑通。

### ③ 验收与现状

| 验收项 | 对应实验 | 状态 |
|---|---|---|
| 压缩不丢关键信息 | ① tmaze（准确率 ~1.0） | ✅ **已跑，100%** |
| token 真的是平的 | ① tmaze token 图 | ✅ **已跑，恒 40** |
| 压完还能认出同处境 | ③ sep-AUC | ✅ **已跑，0.997** |
| 价值信号可信 | ② 信号可信度检验 | 🔧 **脚本已写待跑** |

**出口条件**：② 的 AUC > 0.8，① 的 token 曲线是平的。

**⇒ 阶段 1 只差实验②，脚本已就绪，有 GPU 就能跑。**

## 阶段 2 · 离线跑通 CA（仍不训练）

### ① 目的

**确认压缩后的记忆真的能把功劳分对**。这是方法能不能立住的关键。

### ② 做什么

接上 `SoftGrouper` + `CreditAssigner`，在**离线 dump 的真实轨迹**上算优势，直接量信用精度。

### ③ 前置条件

⬜ **M1：从 verl-agent dump 出真实的 `mem_records.jsonl`。**

> **M1 是阶段 1 到阶段 2 的闸门**：合成数据只能验机制（合成诊断已经踩过 7 个测量假象），**进论文的证据必须来自真实 memory**。

### ④ 验收

| 验收项 | 对应实验 | 状态 |
|---|---|---|
| 关键步的信用排第 1 | ④ credit_rank | ⚠️ 合成数据上部分达成 |
| 保真度加权有净增量 | ⑤ CA 消融（c 臂 > b 臂） | 🔧 **脚本已写待跑** |
| 多个候选关键步里挑得对 | ★ 多线索 T-maze | 🔧 **脚本已写待跑** |

**出口条件**：④ 里 vtree 排第 1，⑤ 里 c > b。

> **这两条过了，方法就算立住了**，剩下的都是工程。

## 阶段 3 · 接进训练

### ① 目的

**证明这套东西能让 agent 变强**，不只是「记忆变短了」。

### ② 做什么

`VTreeMemory` 换进 verl-agent，跑 GRPO 训练。

**算力安排**：先 Qwen2.5-1.5B（对齐 HGPO 设置），跑通再上 7B。

### ③ 验收

| 验收项 | 指标 | 对应动机 |
|---|---|---|
| **agent 更强** | ALFWorld / WebShop 成功率 **> GiGPO / BiPACE**（不是「不掉点」） | **动机二** |
| 真省 token | 平均 context 长度显著低于 baseline，且**随任务长度不涨** | 动机一 |
| **长程优势拉大** | 按任务长度分档：轨迹越长，与 baseline 的差距应**越大** | 动机二 |
| 消融成立 | 去掉保真度加权 / 去掉软分组 → 掉点 | 那四条改法不是白加的 |

**第一行是攻不是守**：如果只做到「成功率持平、token 省了」，那只兑现了动机一——压缩是**必需品**。要兑现动机二（压缩是**增益**），成功率必须**超过** baseline。

**第三行是动机二最锋利的证据**：全历史的冗余量随轨迹长度增长，干扰也随之加重。所以「**轨迹越长、我们赢越多**」这条曲线，比单点成功率更能说明「是压缩提纯了信号」，而不是碰巧调参调赢了。

---

# 第六部分 · 现在就能开工的顺序

| 序 | 干什么 | 产出 | 依赖 | 状态 |
|---|---|---|---|---|
| 1 | `vtree_compressor.py` 抽成共用模块 | 一个类，离线在线都能用 | 无 | ✅ **已完成** |
| 2 | 用它重写实验脚本 | 所有脚本共用同一份压缩实现 | 1 | ✅ **已完成** |
| 3 | `credit.py`：软分组 + CA | 一个模块 + 实验⑤的四条臂 | 无 | ✅ **已完成** |
| 4 | 四个实验脚本 | ② ⑤ ★ 和 §7.4 预检 | 1, 3 | ✅ **已写, dry-run 通过** |
| **5** | **跑实验②**（`signal_check_test.py`） | **地基验证** | GPU | ⬜ **← 建议从这开始** |
| 6 | 跑实验⑤ + ★ | CA 侧结论 | GPU | ⬜ |
| 7 | **M1：verl-agent dump 真实轨迹** | `mem_records.jsonl` | 无（可并行） | ⬜ |
| 8 | 用真实数据重跑 ② ⑤ | **进论文的证据** | 7 | ⬜ |
| 9 | `VTreeMemory` 接进 verl-agent | 阶段 3 起步 | 1, 3 | ⬜ |

## 为什么建议从实验② 开始（而不是 M1）

**上一版这里写的是「从 M1 开始」，理由是实验②要真实环境。写脚本时发现这个判断不对。**

实验②验的是「跳幅大的步是不是真的重要」。原本设想的做法是「把关键步的动作换成随机动作，看成功率掉多少」——那确实要能重置环境。

但**有一个等价且不需要环境的做法**：把那一步的观测**从记忆里抹掉**，看最终决策的准确率掉多少。

```
真实重要性 imp[t] = acc(完整记忆) − acc(抹掉第 t 步)
```

这是个**操作性定义**——不问「我觉得哪步重要」（会变成自证），而问「拿掉它会不会出事」。合成环境里就能做，**不用等 M1**。

**所以顺序变成**：

| | 做什么 | 为什么排这个位置 |
|---|---|---|
| **先** | 跑实验② | 它是地基。**如果跳幅是噪声，M1 做完也白搭** |
| **再** | 跑 ⑤ 和 ★ | 有 GPU 就能跑，不必等 |
| **同时** | 开 M1 | 不依赖任何东西，可并行 |
| **最后** | 用真实数据重跑 | ⚠️ **合成结论只能验机制，进论文的证据必须来自真实 memory** |

> **别把顺序搞反**：先花一周做 M1，结果实验②告诉你信号不可信——那一周就白费了。**先验地基，再铺路。**

---

# 第七部分 · token 级：把「保信用」贯彻到底

> **这一节回答**：在「**怎么压**」这一侧，我们还有没有贡献？
>
> **结论**：**有，而且不需要新机制**——把段落级已经在用的原则往下再用一层。

## 7.1 现有文本压缩在优化什么

⚠️ 以下按公开知识整理，细节以原文为准。

| 方法 | 保留什么 | 判据 |
|---|---|---|
| Selective-Context | self-information 高的内容 | −log p(token) 大 = 罕见 |
| LLMLingua | perplexity 高的 token | 小模型不好预测的 |
| LongLLMLingua | 与 question 相关的内容 | question-aware 重排 |

**共同点：目标是「保住语言信息量」或「与查询的相关性」。**

## 7.2 这个目标和信用分配对不上

ALFWorld 的例子（任务：加热番茄）：

| 观测 | 按信息量 | 按信用 |
|---|---|---|
| "you see a **apple** 1, a **bread** 1, a **butterknife** 1, a **cup** 1…" | 一堆低频名词 → 信息量高 → **保留** | 与番茄无关 → **全是噪声，该删** |
| "you **pick up** the tomato." | 极常见短句 → perplexity 低 → **可能删掉** | **关键动作** → **必须保** |

**判据反着来**：因为「罕见」和「对成败重要」是两回事——环境描述里的杂物名最罕见，对信用却毫无贡献。

## 7.3 我们的做法：换掉目标，不换机制

| 粒度 | 判据 | 决定什么 |
|---|---|---|
| **段落级** | 价值跳幅 | 哪一段压多短 |
| **token 级** | 信用相关性 | 段内保哪些字 |

**token 级的实现就是 `LLMSummarizer` 的一行 prompt**（已在代码里）：

```
"把下面这条 agent 观测压缩成不超过 {n} 个字，只保留会影响任务成败的信息…"
                                          ↑ 目标换在这里
```

**关键区别不在「用不用模型」，而在【问模型什么】**：

| | 问什么 | 得到什么 |
|---|---|---|
| Selective-Context / LLMLingua | （不问，直接算）这个词好不好猜？ | 罕见的词 |
| 普通 LLM 摘要 | 这段话主旨是什么？ | 内容摘要 |
| **本方法** | **哪些内容会影响任务成败？** | **成败判断的依据** |

**论文里的说法**：

> 现有 prompt compression 以「保留信息量」或「与查询相关性」为目标；我们指出在 agentic RL 场景下目标应当是**保留信用相关性**，并在两个粒度上贯彻——段落级按价值跳幅分配 token 上限，token 级以信用相关性为压缩指令。

**这个 claim 不需要任何新机制就成立**，同一原则贯穿两层，方法是统一的而非拼凑。

## 7.4 一个便宜的预检（决定这一节值不值得做）

在投入之前，花约一小时确认「判据确实不一致」：

> 取一批真实观测，分别按**信息量**和**信用相关性**给 token 排序，算两个排序的 Spearman 相关性。
>
> - 相关性低（< 0.3）→ 判据确实不同 → 7.3 的 claim 有实证支撑，写进论文；
> - 相关性高 → 现成方法已经够用 → 这一节降级为一句 related work。

**排序**：放在主线（实验④⑤）之后。**段落级的核心主张还没验完之前，不开第二条战线。**

---

# 第八部分 · 踩过的坑 & 还没定的事

## 8.1 已经踩过并修好的坑

这些都是**自测揪出来的**，不是想出来的——记在这里是为了别再踩：

| # | 坑 | 症状 | 修法 |
|---|---|---|---|
| 1 | 当前观测占历史预算 | **预算越紧越丢关键步**（反向行为） | 当前步开销单独记 |
| 2 | 平局时最早的步永远胜出 | 近处的步系统性吃亏 | tie-break 改靠后优先 |
| 3 | 贪心把预算花在零跳幅步上 | 关键步反而没预算 | 加 `min_jump` 阈值 + break |
| 4 | 摘要器匹配不上时退化成原样 | **S 档形同虚设，三档塌成两档** | 强制保证压缩比 |
| 5 | `ScriptedProbe` 只支持批量 | 在线 push 时跳幅恒为 0 | 加游标 |
| 6 | 用 mean 池化编码 memory | **软分组完全失效** | 改 last-token |

## 8.2 被实测推翻的判断

**列出来是因为它们有共同的毛病，值得警惕：**

| # | 我的判断 | 实测 | 错在哪 |
|---|---|---|---|
| 1 | 探针只占 1~3% | **48%** | 以为 decode 是瓶颈，长记忆下 prefill 才是 |
| 2 | 抽取式成本「趋近 0」 | 有 183ms 固定开销 | 忽略了固定开销 |
| 3 | τ 该放宽 | **方向未定** | 只顺着一个视角推，没考虑坍缩视角 |
| 4 | 折叠标记是噪声，去掉更好 | 去掉**更差** | 它携带时序信息 |
| 5 | 判读只看 sep-AUC | 真正判据是**相似度差距** | AUC 只看排序，排序对 ≠ 拉得开 |
| 6 | 自适应压缩损害一致性 | trunc4 一致性最低却表现更好 | 一致性不等于有用 |

**共同的毛病**：**每一次都是顺着一个角度推到底，没去找反例。** 而且推理链条读起来都很顺——**越顺的推理越容易蒙混过关，因为它没有卡点。**

## 8.3 还没定死的事

| # | 问题 | 现状 | 什么时候必须定 |
|---|---|---|---|
| 1 | **保真度的档位分（1.0/0.6/0.3）** | 拍脑袋 | 实验⑤之前。**如果⑤证明保真度加权没用，这个问题自动消失** |
| 2 | **「压得多狠」怎么量** | 档位 + 长度加权 | 同上。**已确定不能用语义相似度**（番茄→茄子反例） |
| 3 | **τ 往哪调** | 未定，两个方向都要扫 | 阶段 2 |
| 4 | **探针 prompt 措辞** | 用当前这版 | 实验②里顺便扫 2~3 种，选完固定 |
| 5 | **KV cache 复用能否在 verl-agent 实现** | 未验证 | 阶段 3 之前。**有两条退路**（3.4 结论 3） |

## 8.4 已经解决的老问题（存档）

- ~~模板摘要的规则依赖环境句式~~ → **已解决**：改成可插拔接口，默认通用实现，手写规则降级为消融基线。

  > **教训**：凡是「每个环境都要重写一遍」的组件，**都不该长在方法主干上**。
