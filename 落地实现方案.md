# 落地实现方案 —— 从想法到能跑的系统

> 配套 `memory压缩.md`（研究方案：为什么这么做）。本文只讲**怎么实现、接在哪、分几步落地**。
> 目标：照着这份文档能把系统搭起来并跑通训练。

---

## 0. 一条决定架构的硬约束

**两条动机都指向同一个架构结论**（详见 `memory压缩.md` §1.2）：

- **动机一（省 token）**：context 有硬上限，长轨迹必然超限；
- **动机二（提纯信号）**：全历史的冗余会主动干扰信用分配 —— 已有三条证据（sep-AUC 0.52→0.93、vtree 0.969 > raw 0.939、MemoPilot 的 Full History 更差）。

推论：如果压缩只用来算信用、而 agent 决策时仍然看完整历史，那么**动机一失效**（agent 照样超限），**动机二也只兑现一半**（agent 决策时仍被冗余干扰）。

$$\Rightarrow\ \textbf{压缩必须作用在 agent 决策所看的 memory 上}\ \Rightarrow\ \textbf{必须在线压（边走边压），不能事后压}$$

这条把下面所有设计定死了：探针要能增量算、预算重分配要够便宜、摘要不能调模型。

---

## 1. 系统总览

```
                       ┌─────────── 每一步循环 ───────────┐
                       │                                  │
   环境 ──obs_t──►  ①价值探针 ──V_t──► ②跳幅 Δ_t ──► ③预算分配 ──► ④渲染
                       │   (1次轻forward)                        │      │
                       │                                    memory_t   conf_t
                       │                                         │      │
                       │                    agent(actor LLM) ◄───┘      │
                       │                         │                      │
                       └──────── a_t ────────────┘                      │
                                                                        │
   ═══ 轨迹结束 ═══                                                      │
                                                                        ▼
   ⑤表示 φ(memory) ──► ⑥软分组(权重 w) ──► ⑦conf加权(w')  ◄─────────────┘
                                                │
                                                ▼
                                         ⑧优势 A = Q̂ − V̂
                                                │
                                                ▼
                                         ⑨GRPO 更新 actor
```

**①–④ 在 rollout 内、每步执行**（在线压缩）；**⑤–⑨ 在 rollout 后、批量执行**（信用分配 + 训练）。

---

## 2. 关键设计决策（含取舍）

| # | 决策 | 定案 | 为什么 / 代价 |
|---|---|---|---|
| **D1** | 压缩在线还是离线 | **在线**，每步更新 | §0 的硬约束。代价：每步多一次探针调用 |
| **D2** | 探针用哪个模型 | **actor 自己**（复用已加载的权重，`output_hidden_states=False`，只取最后一个 logit） | 不额外占显存；若 actor 太大可换 1.5B 小模型当探针，但要保证**训练全程同一个探针**（否则信号漂移） |
| **D3** | 每步重算所有前缀吗 | **不**，增量：第 $t$ 步只算 $V_t$ 一次 | $O(T)$ 而非 $O(T^2)$ 次调用。这是在线可行的关键 |
| **D4** | 中间档摘要怎么生成 | **可插拔 `Summarizer` 接口**。默认 `TruncSummarizer`（通用兜底、零成本）；质量不够换 `LLMSummarizer`（通用）。手写规则只留作消融基线 | ⚠️ **早期定案是"手写规则模板"，那等于每个环境都要重新实现一整档 → 方法失去通用性**。见下方专段 |
| **D5** | 预算重分配的时机 | **每步重排**（纯 CPU：排序 + 贪心） | 新的一步可能挤掉旧的高跳幅步，必须重排。成本可忽略（$T\log T$） |
| **D6** | $\mathrm{conf}$ 怎么定义 | 由该步**最终落在哪一档**决定：原文 1.0 / 摘要 0.6 / 合并 0.3 | 树的副产物，零额外成本（§3.2② 的实现） |
| **D7** | 训练时 actor 看到什么 | **压缩后的 memory**（和部署一致） | 避免 train-test gap；也让"省 token"的动机在训练里就兑现 |

### D2 的成本论证（决定这方案可不可行）

每步的额外开销 = **一次 forward，只读最后一个位置的 logit**（不生成）。

对比：agent 决策本身要**生成**几十个 token（每个 token 一次 forward + KV cache）。

$$\frac{\text{探针成本}}{\text{决策成本}} \approx \frac{1}{\text{生成长度}} \approx \frac{1}{30\sim 100}$$

→ **额外开销 1~3%**，可以接受。（实测在实验②里一并测出来。）

### D4 的定位：别把两件事混成一件 ⚠️

这里踩过一个坑，值得单独记：最初把中间档写成**手写正则模板**，理由是"在线不能为摘要调 LLM"。但那意味着**换一个环境就要重写一整档**——三档之一要人工适配，方法就没有通用性可言。

问题出在把两件事混成了一件：

| | 是什么 | 谁的活 |
|---|---|---|
| **"这一段该压多短"** | 按价值跳幅分配 token 预算 | ★ **本方法的创新** |
| **"怎么把一段话压短"** | 文本压缩 | **已有成熟通用方案**（LLMLingua、Selective-Context，或直接让 LLM 摘要） |

**正确做法：第二件做成可换的组件。** 换环境不改本方法，只换（或不换）摘要器。论文里的说法：*摘要器与本方法正交，我们贡献的是分辨率分配策略*——通用性问题消失，创新点反而更聚焦。

**成本重算**（原来"不能调 LLM"的理由也站不住）：

- 只有**落在中间档**的步需要摘要——FULL 用原文、MERGE 折叠成一句，都不需要；
- 中间档步数**有界**：$\le$ 预算 $/$ 平均摘要长度；
- 结果**进缓存**，档位不变不重算。

$$\Rightarrow\ \text{每步摊销} \le 1 \text{ 次短生成}\ (\text{不是 } T \text{ 次})$$

**已验证**（`vtree_compressor.py` 自测场景 3）：`TruncSummarizer` 与 `RuleSummarizer` 跑同一条轨迹，**档位序列完全一致**（`FMMFMMSMMF`）——摘要器换实现不影响分辨率分配，正交性成立。

---

## 3. 模块接口

### 3.1 `ValueProbe` —— 价值信号

```python
class ValueProbe:
    """问 actor: 当前 memory 下，这任务能成吗 → P(是)。只做 1 次 forward，不生成。"""
    def __init__(self, model, tok, yes="是", no="否", max_len=2048):
        self.model, self.tok = model, tok
        self.yes_id = tok(yes, add_special_tokens=False).input_ids[0]
        self.no_id  = tok(no,  add_special_tokens=False).input_ids[0]
        self.max_len = max_len

    @torch.no_grad()
    def __call__(self, mem_texts: list[str], task: str) -> np.ndarray:
        prompts = [f"任务:{task}\n当前记忆:\n{m}\n问:这个任务最终能成功吗?只回答一个字:是 或 否。\n答:"
                   for m in mem_texts]
        # 批量 forward, 取末位 logit 上 yes/no 的 softmax
        ...
        return p_yes          # shape (len(mem_texts),)
```

**要点**：训练全程冻结这个探针的行为（不随 actor 更新而改）——否则 $V_t$ 的尺度会漂，跳幅就没有可比性。
**实现**：训练开始时 `deepcopy` 一份 actor 权重当探针，或用独立小模型。**这一条不做，跳幅信号会在训练中途失效。**

### 3.2 `VTreeCompressor` —— 核心压缩模块

```python
RES_FULL, RES_SUMM, RES_MERGE = 0, 1, 2          # 三档
CONF = {RES_FULL: 1.0, RES_SUMM: 0.6, RES_MERGE: 0.3}

class VTreeCompressor:
    """在线增量的价值失真记忆树压缩。每步 push 一次, 随时 render 出当前 memory。"""

    def __init__(self, probe, tok, budget=512, task=""):
        self.probe, self.tok, self.budget, self.task = probe, tok, budget, task
        self.steps  = []        # 每步原文
        self.jumps  = []        # 每步跳幅 Δ_t
        self.v_prev = 0.5       # 先验
        self._res   = []        # 每步最终档位(render 时填)

    def push(self, obs_text: str):
        """走一步: 算 V_t → 跳幅 → 入缓冲。1 次探针调用。"""
        self.steps.append(obs_text)
        cur = self._render_raw()                      # 用原文算价值(不受压缩影响)
        v = float(self.probe([cur], self.task)[0])
        self.jumps.append(abs(v - self.v_prev))
        self.v_prev = v

    def render(self) -> tuple[str, list[float]]:
        """按固定预算贪心分配分辨率 → (memory文本, 每步conf)。纯 CPU。"""
        n = len(self.steps)
        res = [RES_MERGE] * n
        res[n-1] = RES_FULL                            # 当前步必须全貌
        order = sorted(range(n), key=lambda i: -self.jumps[i])
        used  = self._cost(res)
        for i in order:                                # 跳幅大的优先升档
            if res[i] == RES_FULL: continue
            for target in (RES_FULL, RES_SUMM):
                trial = res.copy(); trial[i] = target
                c = self._cost(trial)
                if c <= self.budget:
                    res, used = trial, c
                    break
        self._res = res
        return self._compose(res), [CONF[r] for r in res]

    # ── 三档渲染 ────────────────────────────────────────────
    def _text(self, i, r):
        if r == RES_FULL:  return self.steps[i]
        if r == RES_SUMM:  return self._summ(i)                      # D4: 可插拔摘要器
        return None                                                  # MERGE: 交给 _compose 合并

    def _compose(self, res):
        """连续的 MERGE 段捏成一句 → 这就是树的中间节点。"""
        out, run = [], 0
        for i, r in enumerate(res):
            if r == RES_MERGE: run += 1; continue
            if run: out.append(f"(此处 {run} 步例行操作已折叠)"); run = 0
            out.append(self._text(i, r))
        if run: out.append(f"(此处 {run} 步例行操作已折叠)")
        return "\n".join(out)

    def _cost(self, res):  return len(self.tok(self._compose(res), add_special_tokens=False).input_ids)
    def _render_raw(self): return "\n".join(self.steps)
```

**摘要器**（D4）——可插拔，与本方法正交：

```python
class Summarizer:                 # 接口: 把 text 压到 <= budget_chars, 自带缓存
    def __call__(self, text, budget_chars) -> str: ...

class TruncSummarizer(Summarizer):  # 通用兜底: 保首句主干+截断。零依赖, 先跑通用它
class LLMSummarizer(Summarizer):    # 通用默认: 让 LLM 摘要, 换环境不改代码
class RuleSummarizer(Summarizer):   # 手写正则: 【不通用】, 只作消融基线
```

目标长度 = `ratio × 原文长度`，**必须保证压缩比**——否则摘要 ≈ 原文，S 档形同虚设，三档会塌成两档（自测踩到过）。

### 3.3 `SoftGrouper` + `CreditAssigner` —— §3 的四条改法

```python
class SoftGrouper:
    """§3.2①: memory 表示相似度做核加权, 不硬分组。"""
    def weights(self, phi: np.ndarray, tau=0.1) -> np.ndarray:
        S = phi @ phi.T                                 # 已 L2 归一化
        W = np.exp((S - 1.0) / tau); np.fill_diagonal(W, 0.0)
        return W / W.sum(1, keepdims=True).clip(1e-8)

class CreditAssigner:
    """§3.2②③: conf 加权 + 动作反事实。"""
    def advantages(self, W, conf, returns, actions, use_conf=True, use_pace=True):
        if use_conf:                                    # ② 压得粗的邻居降权
            W = W * conf[None, :]
            W = W / W.sum(1, keepdims=True).clip(1e-8)
        V = W @ returns                                 # V̂(s) = Σ w'_j R_j
        if not use_pace:
            return returns - V
        Q = np.zeros_like(V)                            # ③ 同动作邻居 → Q̂(s,a)
        for i in range(len(V)):
            m = (actions == actions[i]); m[i] = False
            Q[i] = (W[i] * m) @ returns / max((W[i] * m).sum(), 1e-8) if m.any() else returns[i]
        return Q - V
```

**`use_conf` / `use_pace` 两个开关就是实验⑤的四条臂**（见 `memory压缩.md` §7⑤）——消融不用另写代码。

---

## 4. 接进 verl-agent

| 挂载点 | 原来 | 换成 |
|---|---|---|
| `agent_system/memory/` | `SimpleMemory`（最近 K 步原样拼接） | **`VTreeMemory`**：包一层 `VTreeCompressor`，`step()` 里 `push` + `render` |
| `env_manager.build_text_obs` | 拼原始 K-window | 拿 `VTreeMemory.render()` 的结果 |
| 优势计算（GiGPO step-level） | 精确哈希分组 + 组内归一 | **`SoftGrouper` + `CreditAssigner`** |
| rollout 数据结构 | `(obs, action, reward)` | 多带两列：**`conf`（每步）**、`jump`（每步，便于诊断） |

**改动量**：memory 侧一个新类 + 优势计算换一个函数；rollout 结构加两列。**不动训练主循环。**

---

## 5. 三阶段落地路线

### 阶段 1 · 离线跑通压缩（不训练）✅ 部分已完成

**做什么**：用已有的合成 / 真实轨迹，把 `VTreeCompressor` 跑通，确认压缩本身是对的。

| 验收 | 对应实验 | 状态 |
|---|---|---|
| 压缩不丢关键信息 | ① tmaze（准确率 ~1.0） | ✅ 已跑（旧版） |
| token 真的是平的 | ① tmaze token 图 | ⬜ 已改完待跑 |
| 价值信号可信 | ② 信号可信度检验 | ⬜ 待建 |
| 压完还能认出同处境 | ④ value_tree（sep-AUC） | ✅ 0.969（旧版两档） |

**出口条件**：② 的 AUC > 0.8（信号站得住），① 的 token 曲线是平的。

### 阶段 2 · 离线跑通 CA（仍不训练）

**做什么**：接上 `SoftGrouper` + `CreditAssigner`，在**离线 dump 的真实轨迹**上算优势，直接量信用精度。

| 验收 | 对应实验 |
|---|---|
| 关键步的信用排第 1 | ③ credit_rank |
| conf 加权有净增量 | ⑤ CA 消融（c 臂 > b 臂） |
| 多个候选关键步里挑得对 | ★ 多线索 T-maze |

**前置**：M1（verl-agent dump 真实 `mem_records.jsonl`）。

**出口条件**：③ 里 vtree 排第 1，⑤ 里 c > b。**这两条过了，方法就算立住了**，剩下的是工程。

### 阶段 3 · 接进训练

**做什么**：`VTreeMemory` 换进 verl-agent，跑 GRPO 训练。

| 验收 | 指标 | 对应动机 |
|---|---|---|
| **agent 更强** | ALFWorld / WebShop 成功率 **> GiGPO / BiPACE**（不是"不掉点"） | **动机二** |
| 真省 token | 平均 context 长度显著低于 baseline，且**随任务长度不涨** | 动机一 |
| **长程优势拉大** | 按任务长度分档统计：轨迹越长，与 baseline 的差距应**越大** | 动机二（冗余越多、干扰越重） |
| 消融成立 | 去掉 conf / 去掉软分组 → 掉点 | §3 那四条不是白加的 |

**第一行是攻不是守**：如果只做到"成功率持平、token 省了"，那只兑现了动机一——压缩是**必需品**。要兑现动机二（压缩是**增益**），成功率必须**超过** baseline。

**第三行是动机二最锋利的证据**：全历史的冗余量随轨迹长度增长，干扰也随之加重。所以"轨迹越长、我们赢越多"这条曲线，比单点成功率更能说明"是压缩提纯了信号"，而不是碰巧调参调赢了。

**算力**：先 Qwen2.5-1.5B（对齐 HGPO 设置），跑通再上 7B。

---

## 6. 现有实验在路线里的位置

之前的四五个脚本不是散的，**它们是这条路线上的验收点**：

```
阶段1 (压缩对不对)          阶段2 (信用准不准)         阶段3 (训练涨不涨)
├─ ① tmaze  ✅改完           ├─ ③ credit_rank           └─ ALFWorld/WebShop
├─ ② 信号检验                ├─ ⑤ CA 消融                   成功率 + token + 消融
└─ ④ value_tree 升级         └─ ★ 多线索 tmaze
        │                            │
        └────── M1 真实 memory ──────┘
```

**M1 是阶段1到阶段2的闸门**：合成数据只能验机制（旧诊断踩过 7 个测量假象），进论文的证据必须来自真实 memory。

---

## 7. 现在就能开工的顺序

| 序 | 干什么 | 产出 | 依赖 |
|---|---|---|---|
| 1 | `vtree_compressor.py`：把 §3.1+3.2 写成能 import 的模块 | 一个类，离线在线都能用 | 无 |
| 2 | 用它重写 `tmaze_text_test.py` / `value_tree_test.py` | 两个脚本共用同一份压缩实现（现在是各写各的） | 1 |
| 3 | 实验② 信号检验 | 地基验证 | 1 |
| 4 | M1：verl-agent dump 真实轨迹 | `mem_records.jsonl` | 无（可并行） |
| 5 | `credit.py`：§3.3 的分组 + CA | 一个模块 + 实验③⑤ | 4 |
| 6 | `VTreeMemory` 接进 verl-agent | 阶段3 起步 | 1,5 |

**第 1 步最关键**：现在两个脚本各自实现了一遍压缩逻辑（`mem_vtree` 各写各的），**必须先抽成一个共用模块**，否则后面每加一个实验就分叉一次，接进 verl-agent 时又要重写第三遍。

---

## 8. 还没定死的两处（实现时会撞上）

1. ~~模板摘要的具体规则依赖环境句式~~ → **已解决**：摘要器改成可插拔接口（D4），默认用通用实现，手写规则降级为消融基线。**教训**：凡是"每个环境都要重写一遍"的组件，都不该长在方法主干上。

2. **探针的 prompt 措辞** —— "这个任务最终能成功吗"在不同环境下的敏感度不同。**建议：在实验②里顺便扫 2~3 种措辞，选 AUC 最高的固定下来，之后不再改。**
